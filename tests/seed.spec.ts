import { test } from './fixtures.ts';

test('seed', async () => {
  // The fixture already resets state and navigates. This empty body is where
  // `--debug=cli` pauses, so exploration and generation start from a clean app.
});
